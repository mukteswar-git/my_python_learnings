* pip upgradatipon : python -m ensurepip --upgrade / python -m pip install --upgrade pip

* tailwind installation:
1. pip install django-tailwind, pip install django-tailwind[reload]
2. Create Tailwind App
python manage.py tailwind init
3. Update settings.py
INSTALLED_APPS = [
    ...
    'tailwind',
    'theme',
    'django_browser_reload',
]
NPM_BIN_PATH = "C:/Program Files/nodejs/npm.cmd"
TAILWIND_APP_NAME = 'theme'
INTERNAL_IPS = ['127.0.0.1']

MIDDLEWARE = [
    ...
    "django_browser_reload.middleware.BrowserReloadMiddleware",
]

4. tailwind install
python manage.py tailwind install

5. Start tailwind
python manage.py tailwind start


6. media configuration 
import os
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

7. static configuration
import os
'DIRS': ['templates'], # inside template section

STATIC_URL = '/static/' # below this add the following line
STATICFILES_DIRS = [os.path.join(BASE_DIR, 'static')]

- configure MEDIA_URL
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),
    #...
    #...
] + static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)


